import 'package:flutter/material.dart';

class categoryType {
  final String id;
  final String title;
  final Color color;
  const categoryType(
      {required this.id, required this.title, this.color = Colors.red});
}
